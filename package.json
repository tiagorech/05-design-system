{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
     "dev": "turbo run dev --parallel",
     "build": "turbo run build",
     "lint": "turbo run lint"
   },
  "devDependencies": {
    "tsup": "^8.4.0",
    "turbo": "^2.5.2"
  },
  "dependencies": {
    "polished": "^4.3.1"
  },
  "packageManager": "npm@10.5.0",
  "name": "with-vite",
  "engines": {
    "node": ">=20.0.0"
  }
}
